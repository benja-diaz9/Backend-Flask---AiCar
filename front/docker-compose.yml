version: '3.8'
services:
  front:
    build: .
    container_name: aicar-front
    ports:
      - "5173:5173"
    environment:
      - VITE_BACKEND_URL=${VITE_BACKEND_URL:-http://localhost:8080}
    restart: unless-stopped

